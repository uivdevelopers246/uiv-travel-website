/** MVP cart line type for activity slots; matches `cart_lines.line_type` CHECK. */
export const CART_LINE_TYPE_ACTIVITY = "activity" as const;

/** Cart line type for accommodation stays; matches `cart_lines.line_type` CHECK. */
export const CART_LINE_TYPE_ACCOMMODATION = "accommodation" as const;
